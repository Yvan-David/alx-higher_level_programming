#!/usr/bin/python3
"""
a module for isinstance
"""


def is_same_class(obj, a_class):
    """
    a function for isinstance
    """

    if (isinstance(obj, a_class)):
        return (True)
    else:
        return (False)
